schema_version = 1

[[subset]]
interface = "GPUDevice"
members = ["createSampler"]

[[subset]]
interface = "GPUSampler"
members = ["label"]

[lifecycle]
standard_interfaces = ["GPUSampler"]

[[descriptor]]
dictionary = "GPUSamplerDescriptor"

[[descriptor.strings]]
member = "label"
nullable = false
