# Execution-result families. These verify what the GPU actually COMPUTED — buffer
# contents after a dispatch, pixels after a draw, memory visibility across passes.
#
# They require a real backend and are DAWN-ONLY BY CONSTRUCTION. yawgpu's Noop
# backend executes nothing, so every case fails with "Array had unexpected
# contents"; that is not a binding bug and no expectation can express it.
#
# Run: cts-runner built --no-default-features --features backend-dawn, against a
# local Dawn. Gated, real GPU, never CI. See CLAUDE.md -> "The oracle protocol".
#
# Screened 2026-07-14 on Dawn, all green:
webgpu:api,operation,compute,*
webgpu:api,operation,sampling,*
webgpu:api,operation,render_pass,*
webgpu:api,operation,render_pipeline,*
webgpu:api,operation,resource_init,*
webgpu:api,operation,memory_sync,*
webgpu:api,operation,storage_texture,*

# Added 2026-07-14 after the binding fixes below.
# buffers: mappedAtCreation OOM is a RangeError, and device.destroy() detaches
#   outstanding mapped ranges (a soundness bug: script held a live ArrayBuffer over
#   freed GPU memory).
# rendering: GPUExternalTexture's interface object was missing. 390 cases in
#   3d_texture_slices — nothing to do with external textures — failed on a shared
#   helper's `texture instanceof GPUExternalTexture` brand check.
webgpu:api,operation,buffers,*
webgpu:api,operation,rendering,*

# Phase B-10 (2026-07-14). command_buffer, screened sub-family by sub-family (the whole
# family times out as one query — 3,700 execution-result cases).
#
# NOT added: programmable,immediate — immediate data is unimplemented (recorded).
# queries carries six expectations for D15 (Dawn/Metal's 32-buffer timestamp limit).
webgpu:api,operation,command_buffer,basic:*
webgpu:api,operation,command_buffer,clearBuffer:*
webgpu:api,operation,command_buffer,copyBufferToBuffer:*
webgpu:api,operation,command_buffer,image_copy:*
webgpu:api,operation,command_buffer,render,*
webgpu:api,operation,command_buffer,queries,*
webgpu:api,operation,command_buffer,programmable,state_tracking:*
webgpu:api,operation,command_buffer,copyTextureToTexture:*
